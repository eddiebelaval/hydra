# HYDRA v1 Work Layer, retired 2026-07-11

Decision: Eddie, 2026-07-11 12:42 EDT ("retire the v1 layer and start the rewire").
Evidence: ~/Development/id8/EPISTEMIC-MATRIX-2026-07-11-morning-brief.md

## What was retired
- `com.hydra.agent-milo` / `agent-forge` / `agent-pulse` / `agent-scout` launchd daemons:
  unloaded via bootout, plists archived in ./launchagents/. All four had been failing
  (exit code 2) for months while the `agents` table read "active".
- The Morning Instrument no longer renders hydra.db agents/tasks/notifications.
  Petals now read the atlas wire (id8-halos client atlases), launchd fleet health,
  and hydra.db daily_priorities (the one loop kept alive on purpose).

## Why
The work layer completed exactly one task since 2026-02-05 (a smoke test, NULL
completed_at). The real work of the portfolio flows through git, boards, the atlas
health wire, and Claude sessions. The delivery layer (briefing, planner, brain-updater)
stays; it now reports on living sensors.

## What was NOT retired
- hydra.db itself (daily_priorities still read by the planner loop and the instrument;
  other consumers unaudited: dashboard-server, milo-telegram, telegram-listener,
  mara-warroom. See matrix Q2#4).
- morning-planner, daily-briefing, brain-updater, and the rest of the com.hydra.* fleet.
- ~~The markdown briefing template inside daily-briefing.sh still queries the dead tables~~
  DONE 2026-07-12: daily-briefing.sh rewritten; markdown twin + Telegram summary now
  derive from briefing-instrument.py's living sensors. No dead-table queries remain
  in the briefing path.
- Mission Control: DONE 2026-07-12 — /api/tasks/hydra is retirement-aware (this
  marker file IS its signal; relics via ?includeAll=true), the tasks-page panel
  renders the retirement, status.ts agent special-case removed. The 5/6 AGENTS
  chip was a false alarm (MC's own agent registry, not hydra.db).
  Remaining v1 consumers unaudited: dashboard-server (port 1000), milo-telegram,
  telegram-listener, mara-warroom.

## To reverse
Move the plists back to ~/Library/LaunchAgents/ and `launchctl bootstrap gui/$UID <plist>`.

## Green sweep, 2026-07-12 (same session, continued)
- RETIRED additionally: `com.hydra.sync` (the SOLE WRITER to the dead task layer — it re-created the marketing-streak nag daily; retiring it stops the corpse being fed) and `com.id8labs.friday-prep-sync` (canonical FRIDAY-PREP.html no longer exists; May-era relic).
- Last v1 consumers swept: hydra dashboard-server (`~/.hydra/dashboard/generate-data.sh` gated on this marker), telegram-listener v1 commands (tasks/agents/notifications/approve/reject/complete/standup/mention) answer with the retirement. milo-telegram, ava-telegram, mara-warroom verified clean — never consumers.
- Fleet repaired same day: 12 failing jobs -> 2 legitimately-red alarms (dnb-health, halo-nursery-rounds). weekly-backup had been dead since Feb (launchd Standard*Path pointed at iCloud-evicted files — spawn failed before the script ran; runner+logs moved out of iCloud).
